# ArkUI-X 三端能力验证记录

## 基本信息

- 关联决策：`ADR-001`
- 验证日期：2026-08-07（环境预检）
- 目标版本：ArkUI-X `6.0.0 Release`
- 当前状态：`pending`
- 通过条件：本文全部关键项在 HarmonyOS、Android、iOS 上获得可复现证据，且不存在未处理的阻塞问题

## 当前环境预检

| 工具 | 当前结果 | 判定 |
| --- | --- | --- |
| Xcode | 仅发现 `/usr/bin/xcodebuild` 命令入口，未获得完整 Xcode 版本信息 | 未就绪 |
| Java | 未安装可用 Java Runtime | 未就绪 |
| Android ADB | 未发现 `adb` | 未就绪 |
| DevEco Studio | 未发现应用安装 | 未就绪 |
| Android Studio | 未发现应用安装 | 未就绪 |
| ohpm | 未发现 | 未就绪 |
| hvigor | 未发现 | 未就绪 |
| ArkUI-X SDK | 未发现本地 SDK | 未就绪 |

结论：当前只能批准技术方向，不能声称三端构建或关键能力已经验证。安装工具链并准备模拟器或真机后继续执行本记录。

## 验证约束

- 验证工程是正式 `client/` 工程创建前的隔离 PoC，不实现产品完整页面和业务目录。
- 使用合成练字图片，不使用真实学生身份或未经授权的书写数据。
- 每项记录工具版本、设备/模拟器版本、执行步骤、结果和证据位置。
- 任何平台的关键项失败，整体状态不得标记为 `approved`。

## 工具链与启动

| 编号 | 验证项 | HarmonyOS | Android | iOS |
| --- | --- | --- | --- | --- |
| TOOL-01 | 安装并锁定 ArkUI-X 6.0.0 Release | 待验证 | 待验证 | 待验证 |
| TOOL-02 | 创建最小 ArkTS 页面并编译 | 待验证 | 待验证 | 待验证 |
| TOOL-03 | 在最低系统版本启动 | 待验证 | 待验证 | 待验证 |
| TOOL-04 | 在一个当前主流版本启动 | 待验证 | 待验证 | 待验证 |

## 平台能力

| 编号 | 验证项 | 通过标准 |
| --- | --- | --- |
| CAP-01 | 应用内拍照 | 可显示方格辅助层，完成权限申请、拍摄、取消、重拍、旋转处理和图片回传 |
| CAP-02 | 相册选择 | 可通过系统选择器选择图片；拒绝权限或取消时返回可区分结果 |
| CAP-03 | 文件存储 | 可在应用沙箱保存、读取、删除原图和压缩图，能够识别空间不足 |
| CAP-04 | 关系型数据 | 可创建、迁移、写入并在应用重启后读取练习、任务和单字结果 |
| CAP-05 | 偏好设置 | 可保存并读取轻量配置，重启后保持一致 |
| CAP-06 | HTTP 上传 | 可上传图片和元数据，展示进度，并支持取消、超时、失败重试 |
| CAP-07 | 断网恢复 | 离线任务保留为 `local_pending`；联网后使用同一幂等标识继续且不重复提交 |
| CAP-08 | 差异绘制 | 可流畅绘制多字边界框、标准字叠加、差异区域和问题标记 |
| CAP-09 | 响应式布局 | `<600vp`、`600–839vp`、`>=840vp` 三档无溢出、遮挡或关键操作丢失 |
| CAP-10 | 生命周期恢复 | 上传或分析期间终止并重启应用后，可恢复真实任务状态且不重复写入结果 |

每一项必须分别记录 HarmonyOS、Android、iOS 的通过/失败及证据；不得以某一平台结果替代其他平台。

## 异常场景

- 相机或相册权限拒绝、永久拒绝和系统设置后恢复。
- 图片损坏、格式不支持、过大、过暗、模糊和磁盘空间不足。
- 网络断开、超时、服务端 4xx/5xx、响应格式错误和部分单字处理失败。
- 上传期间取消、应用切后台、进程终止和设备旋转。
- 同一任务连续重试，确认服务端和本地均未产生重复问题字记录。
- 系统字体放大、横竖屏、分屏和安全区变化。

## 相机降级判定

任一平台出现以下情况且在限定验证周期内无法通过官方 ArkUI-X 能力稳定解决时，启用独立原生拍摄页：

- 取景画面无法稳定嵌入或旋转方向错误。
- 无法叠加方格辅助层或无法可靠获取拍摄结果。
- 权限、前后台切换或重拍导致崩溃、黑屏或结果丢失。
- 最低系统版本无法满足可接受的拍摄体验。

降级后仍必须通过统一 `CaptureService` 返回图片引用、尺寸、方向和来源，不允许业务页面感知平台差异。

## 最终结论

- 状态：`pending | approved | failed`
- 验证负责人：待安排
- HarmonyOS 证据：待补充
- Android 证据：待补充
- iOS 证据：待补充
- 阻塞问题：当前缺少完整三端工具链、ArkUI-X SDK 和可运行设备/模拟器
- 结论日期：待补充
