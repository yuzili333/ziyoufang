# Include this file inside the existing HTTP server block for lilicoconut.me.
# It redirects only this project's API namespace and leaves other website paths unchanged.
location = /api/v1 {
  return 308 https://$host$request_uri;
}

location ^~ /api/v1/ {
  return 308 https://$host$request_uri;
}
